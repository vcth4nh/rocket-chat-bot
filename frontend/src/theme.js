import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#d32f2f', // Replace with your desired primary color
    },
    secondary: {
      main: '#000000', // Replace with your desired secondary color
    },
  },
});

export default theme;
